# Motion

Motion communicates cause and effect and must not delay routine tasks. Use 120 ms for feedback, 200 ms for standard transitions, and 320 ms for deliberate transitions. Do not use bounce, scale-on-hover, parallax, autoplay, looping decoration, shimmer after loading, or `transition: all`.

Under `prefers-reduced-motion: reduce`, remove nonessential movement and scrolling animation.
