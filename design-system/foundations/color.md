# Color

Use semantic roles, never primitive palette names, in application and component code. The canvas is white, grouped content uses cool-neutral 50, and the primary action/focus color is Aegean 600. Status colors always include text or icon meaning. Dark sections, decorative gradients, glows, and hue-only communication are prohibited.

The canonical values and aliases are in `../tokens/source/tokens.json`.
