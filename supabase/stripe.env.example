# Copy to supabase/.env.stripe.local (Git-ignored). Enter values locally, never in chat.
STRIPE_SECRET_KEY=sk_test_replace_me
STRIPE_WEBHOOK_SECRET=whsec_replace_me
STRIPE_MODE=test
STRIPE_PAYMENTS_ENABLED=false
APP_BASE_URL=http://localhost:5173
# Choose the correct code after reviewing Stripe Tax product classification.
STRIPE_CREDIT_TAX_CODE=
# Set true ONLY if the target is a separate remote TEST Supabase project.
STRIPE_TEST_DATABASE_CONFIRMED=false
