# White frame for the analytics preview

Updated `public/images/home-step-gain-insights-actual.webp` with one rounded white frame/panel enclosing the complete current analytics screenshot, matching the white-panel treatment of previews one and two. The yellow-left to mint-right gradient remains visible around the frame. The enclosing surface uses design-system white (`semantic.color.surface.default`, `#FFFFFF`). The existing screenshot-preview exception records this presentation.

Only the third image asset was replaced. Its dimensions remain 1448 × 1086. The built-in image editing tool produced the edit; the final asset was encoded as WebP.

## Prompt

Use case: precise-object-edit. Edit ONLY the first reference image, the current Gain insights homepage preview. The second reference shows the white screenshot-card framing to match. Add one enclosing rounded WHITE frame/panel around the ENTIRE analytics screenshot content in the first image, so it has the same white-card-on-pastel-background treatment as the second. Use opaque white #FFFFFF (design-system surface.default) behind all the analytics content, with a modest even white margin, softly rounded corners and a very subtle thin #D9DFE3 boundary. The enclosing panel should surround everything from View recordings through Preview report; do not frame only the two video thumbnails. Keep the existing soft yellow-on-LEFT to mint-on-RIGHT gradient visible OUTSIDE this white panel around the image edges. Preserve all current screenshot content exactly: View recordings and arrow, both actual recording thumbnails and play buttons, Transcript report, its current description, Copy report, Download report, Preview report. Do not add obsolete recording labels or transcript counts. Do not flip, rearrange, or redesign the interface. Keep scale and placement wherever possible; add the white panel behind the existing UI with enough inset that all content fits comfortably. Retain final 1448x1086 landscape 4:3 proportions. No extra title, number, icon, decoration, dramatic shadow, new colors, browser chrome or perspective. The second reference supplies only white panel framing; do not copy its Share form content.
