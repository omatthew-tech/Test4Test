# Third preview gradient reversal

Only `public/images/home-step-gain-insights-actual.webp` was replaced: pale yellow (`semantic.color.status.warning.tint`, `#FFF4D6`) now starts at the left and blends gradually into mint (`semantic.color.status.success.tint`, `#E8F7EF`) at the right. The foreground is not mirrored. The first two assets are unchanged.

Edited with the built-in image editing tool and encoded as WebP. Dimensions remain 1448 × 1086. The existing screenshot-preview exception records this requested reversal.

## Prompt

Use case: precise-object-edit. The reference is the exact existing Test4Test Gain insights homepage image. Make ONE change only: REVERSE ONLY ITS BACKGROUND GRADIENT horizontally. The current background has mint at the left and pale yellow at the right. The result must have pale yellow #FFF4D6 at the LEFT edge, slowly and smoothly blending across the entire width into pale mint #E8F7EF at the RIGHT edge. These are the existing design-system warning-tint and success-tint colors. Match the current subtle pastel saturation, softness and broad gradual transition, simply with the gradient direction reversed. Absolutely do NOT mirror, flip, move, crop, retype or redesign any foreground content. Keep original 1448x1086 canvas and all UI pixels, two white recording cards with their actual thumbnails and text, every heading, report copy, links, and buttons at their exact original positions. Opaque finished background, no transparency, no pink, no additional color, no bands, vertical gradient, frame, extra decoration, or text. Only the pastel background's horizontal direction changes; everything else remains visually identical.
